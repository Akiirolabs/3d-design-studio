interface:
  display_name: "Dev Cycle"
  short_description: "Coordinate bounded developer and auditor iterations"
  default_prompt: "Use $dev-cycle to align intent with DevSync, implement with DevBase, and independently verify with DevAudit and DevSync."
